/**
 * Migrations are now managed by Drizzle Kit.
 * Run: npm run migrate
 *
 * Migration files are generated in ./drizzle/ by `drizzle-kit generate`
 * and applied to Neon by `drizzle-kit migrate`.
 */
